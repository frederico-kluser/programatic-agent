# Learnings — surf-plan-skill

Append-only log consumed by meta-skill-evolution and meta-skill-consolidate.
Entry format: `- [YYYY-MM-DD][source:user|inference][task:<slug>][probation] <fact>`
States: probation (default) -> promoted (distilled into SKILL.md by meta-skill-consolidate after dual-buffer check) | superseded (kept for history, never deleted).
Learnings are routed here when THIS skill owns the domain of the fact — regardless of which skill ran the task.
This skill is VENDORED from `surf-skill@5.2.0`; upstream-drift observations belong here too.

<!-- entries below this line -->
- [2026-07-31][source:inference][task:dev-mode-metodologias][probation] On THIS host, the research layers have a working hybrid: `surf-research-skill` CLI v5.0.0 (Layer A) served the synthesis batch directly, and explore-type subagents carrying WebSearch/FetchURL covered baseline + grounding in ONE parallel round (6 agents: 3 codebase + 3 web) — the vendored "no harness WebSearch rung" caveat binds the pi agent inside huu's container, not a host harness that has those tools. Deep-mode clarify also compresses well: a single AskUserQuestion round with multi_select settled 4 register items at once (scope/options/BDD/enforcement), no second round needed, and the answers mapped 1:1 onto plan Decisions.
- [2026-09-05][source:inference][task:onda4-surf-no-container][probation] O PACOTE MUDOU DE NOME e o `Dockerfile` ficou para trás por 3 majors: `npm view surf-skill deprecated` responde "Renomeado para surf-agent-skill — instale com: npm i -g surf-agent-skill", `surf-skill` está congelado em 7.0.0 e o `latest` do nome vivo (`surf-agent-skill`) é 8.0.1. A imagem instalava `surf-skill@5` (range → 5.4.0) enquanto o código chamava a v8 — e como o huu é docker-only, a imagem É a pesquisa em produção. CORRIGIDO para `surf-agent-skill@8` (range de MAJOR: 8.x entra sozinho, v9 não), com três `command -v` no mesmo RUN (`surf-research-skill`, `surf-search-normal`, `surf-search-unlimit`) para que essa classe de bug falhe no BUILD e não no card do agente. Nuance que quase enganou a investigação: `npm view surf-skill@5 bin` lista TODAS as 5.x, e a 5.4.0 já tinha `surf-search-normal`/`surf-search-unlimit` — ou seja, o range 5 NÃO deixava os binários de busca faltando; o erro real era o NOME abandonado, e com ele um CLI pré-v8 que ainda trazia o `surf-free-skill` e a escada sem chave que a v8 apagou.
- [2026-09-05][source:inference][task:onda4-surf-no-container][probation] Fatos da v8 verificados no `--help` do binário 8.0.1 instalado no host (nenhuma busca foi executada): Brave é o ÚNICO backend, sem provider de fallback e sem tier grátis embaixo; `gate` é o único verbo que responde SEM chave e codifica a resposta no exit (0 = há chave · 78 = não há, e sai ANTES de pesquisar, então repetir é garantido falhar de novo); `--version` sai 0 sem chave nenhuma, logo prova instalação e nunca prontidão; `--sub-agents` é o único orçamento de simultaneidade (padrão 10, máx 20; fora de 1..20 sai 2) e `--concurrency` virou alias depreciado; e `extract`, `crawl`, `map`, `research`, `research-start`, `research-poll` e `usage` foram REMOVIDOS na v8 porque a API de busca do Brave devolve links e snippets, nunca conteúdo de página. Consequência para esta skill: a receita "search --max 3-5 + extract as páginas load-bearing" morreu — leia as URLs você mesmo (`curl` existe sempre no container) ou peça a onda `surf-search-normal`, que lê e cita por você.
