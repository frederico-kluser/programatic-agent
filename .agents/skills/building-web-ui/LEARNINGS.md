# Learnings — building-web-ui

Append-only log consumed by meta-skill-evolution and meta-skill-consolidate.
Entry format: `- [YYYY-MM-DD][source:user|inference][task:<slug>][probation] <fact>`
States: probation (default) -> promoted (distilled into SKILL.md by meta-skill-consolidate after dual-buffer check) | superseded (kept for history, never deleted).
Learnings are routed here when THIS skill owns the domain of the fact — regardless of which skill ran the task.

<!-- entries below this line -->
- [2026-06-25][source:user][task:sequential-projects-queue][probation] User wants the web "project selector" to multi-select projects that run SEQUENTIALLY (not async), each with its OWN config (dir/provider/model/concurrency), with each execution archived to IndexedDB and exportable as full JSON incl. per-card and per-project costs; on a mid-queue failure the queue CONTINUES and marks the project failed. Implemented 100% client-side (queue runner in app.js + history store in db.js) — no server change.
- [2026-06-25][source:inference][task:sequential-projects-queue][probation] Browser-driven sequential runs ride the existing single-run server: WebRunManager throws "already in progress" → HTTP 409 while active, then flips phase done/error + nulls the orch on settle, so the next /api/run is accepted. Pattern: on a terminal SSE phase, archive the final `state`, then POST the next run; retry on a transient 409 (≤30× / 350ms covered every observed gap). Guard double-archive with a latched current.runId + a processed-runId Set (SSE replays the last frame to new connections).
- [2026-06-25][source:inference][task:sequential-projects-queue][probation] server.startRun derives the backend from `provider` (openrouter→pi, azure→azure) and only falls back to body.backend when NO provider is sent; it IGNORES opts.lockedBackend. So `--web --stub` does NOT make browser-initiated runs use stub — POST `{backend:'stub'}` with no provider (stub.requiresApiKey=false) to drive a real no-LLM run for local verification.
- [2026-06-25][source:inference][task:sequential-projects-queue][probation] Per-card cost exists ONLY on AgentStatus; StageIntegration (merge) and CheckRun (judge) have no `cost` field — their LLM cost folds into OrchestratorState.totalCost (authoritative project total). A real stub `smoke` run yielded 5 cards (3 agents + 2 merges, 0 judges); the terminal web snapshot preserved every AgentStatus field the archival reads (tokensIn/Out, cacheRead/Write, cost, requeues, branchName, commitSha, filesModified, pushStatus, error).
- [2026-06-25][source:inference][task:sequential-projects-queue][probation] No-browser verification recipe that worked: (1) keep DOM-free logic in its own module (db.js touches indexedDB/document only inside functions) and import it in Node; (2) `fake-indexeddb/auto` in a SCRATCH npm project (never add to project deps) to round-trip the IndexedDB store; (3) boot `--web --stub` + curl assets/`/api/bootstrap` for the ESM graph + content-types; (4) drop the wrapped `.fixtures/smoke-pipeline.json` into `pipelines/` (parsePipelineFromJson accepts `{_format,pipeline}`) and drive `{backend:'stub', runDirectory:<temp git repo>}` to capture a real OrchestratorState. The stub run's worktrees land under the runDirectory's `.huu-worktrees/`, so a temp repo keeps the project clean.
- [2026-06-25][source:inference][task:run-log-realtime-stream][probation] The single `/events` SSE stream now carries TWO frame shapes distinguished by `frame.type`: `run` (throttled state snapshot, coalesced to ≤1/120 ms, last frame replayed on connect, per-agent logs capped at 200) and `agent-stream` (`{agentId,channel,text}`, raw output firehose — NOT throttled, NOT replayed, one frame per coalesced line, mirrored to `console.log`). The server's `broadcastAgentStream` writes the firehose DIRECTLY to every `sseClient` (bypasses the snapshot throttle) and short-circuits when `sseClients.size === 0`. Wiring: `WebRunManager` constructor gained a 3rd arg `onAgentOutput` that subscribes to `Orchestrator.subscribeAgentOutput`; `app.js` `connectSse().onmessage` branches on `frame.type`. Pattern to reuse: append-only / high-frequency data gets its own un-throttled frame type, never bloats the `run` snapshot.
- [2026-06-25][source:inference][task:run-log-realtime-stream][probation] `client/app.js` ships ONLY via the build's `cp -R src/web/client/. dist/web/client/` step (package.json `build`), because the server serves `clientDir()` = `./client/` relative to the module (dev→src, prod→dist). So a client edit is live under `npm run dev` (tsx) instantly but needs `npm run build` to reach the Docker/prod image — `tsc`/tests are GREEN either way (the file isn't compiled), so a stale-UI regression is silent. `node --check src/web/client/app.js` is the only syntax gate for the vanilla-JS client.
- [2026-06-25][source:user][task:openrouter-all-models-freetext][probation] FREE-TEXT model entry in the vanilla combobox (`client/app.js`): the user wanted to "type the model name" for any id, listed or not. Pattern — `customCandidate()` returns `{id:q,label:q,custom:true}` when the trimmed query is non-empty AND not an EXACT catalog id; `comboMatches()` appends it after the substring matches. So a partial like "deepseek" keeps real rows first (Enter picks the first real match) and only a full unlisted id surfaces a lone `Use "<id>"` row that commits verbatim (mousedown reads the row's real `data-id`; the inner `combo__opt-id` text "sent to OpenRouter as-is" is display only). `selectModel(id)` was relaxed from `if (id && !modelById(id)) return;` to `if (!id) return;` — the server never validates `modelId`, pi forwards it straight to OpenRouter. Three follow-on edits make a custom id STICK: `syncModelInput` shows `S.modelId` verbatim when `modelById` misses; `refreshModelsAndKeys` preserves a non-empty `S.modelId` across key/provider refresh (only seeds a default when empty) instead of resetting to `models[0]`; and the provider-switch handler sets `S.modelId=''` so a typed id never leaks across providers. Capability badges replace hiding: `reasoning` (thinking) + a soft `no tools` warning (`combo__badge--warn`) when `m.tools===false`. `node --check` + a tiny Node mirror of `customCandidate`/`comboMatches` is enough to verify the logic with no browser.
- [2026-06-26][source:user][task:openrouter-models-keyless-public-catalog][probation] User wanted the web "Model" picker to show the COMPLETE OpenRouter list the instant they open it, not the ~2 static presets — the presets appeared because the full catalog was gated behind a validated OpenRouter key (`api-data.ts` `if (backend === 'pi' && key)`). OpenRouter's `/models` is public, so the fix is browser-transparent: `refreshModelsAndKeys` already GETs `/api/models`; the server now returns the full 339-model catalog even with no `x-huu-key`. Only client COPY changed: `updateModelCap`'s "recommended" branch no longer says "validate your OpenRouter key to load the full catalog" (now reached only on a real fetch failure → "Couldn't reach OpenRouter — showing recommended models; type any id"), plus the two stale "loaded with the validated session key / filtered to tool+reasoning" comments. Reusable server-test hermeticity pattern: when a server test drives a route that triggers an OUTBOUND fetch AND the test itself uses real `fetch` to reach its own server (so it CAN'T stub global fetch wholesale), wrap `globalThis.fetch` to pass localhost through and canned-respond the external host — `const realFetch = globalThis.fetch; vi.spyOn(globalThis,'fetch').mockImplementation((input,init) => url.includes('openrouter.ai') ? Promise.resolve(new Response(cannedJson)) : realFetch(input,init))`, then `spy.mockRestore()` + `resetCapabilitiesCache()` in `finally`.
- [2026-06-25][source:inference][task:openrouter-all-models-freetext][probation] Latent crash found + fixed in the SAME flow: `editQueueItem` (queue edit) still read `const sel = $('modelSelect'); sel.value = ...` but the combobox migration replaced `#modelSelect` with `#modelInput` — `$()` is `getElementById` so `sel` was `null` and editing any queued project threw `null.value`. Fix: `S.modelId = it.modelId || S.modelId; syncModelInput(); updateModelHint();`. Lesson: after a form-control migration, grep the WHOLE client for the old element id (`modelSelect`) — tsc/vitest never touch `app.js`, so a dead DOM ref is invisible until runtime.
- [2026-06-26][source:inference][task:simulation-mode][probation] `WebRunManager` drives a run through a tiny structural seam — `subscribe(state)` + `subscribeAgentOutput(chunk)` + `start():Promise<{runId,manifest:{errorReason?}}>` — NOT the concrete `Orchestrator`. So a `SimulationEngine` (src/orchestrator/simulation/) that emits the SAME `OrchestratorState` snapshots + `agent-stream` frames plugs into the EXACT same SSE/snapshot machinery with zero client changes. Refactor pattern that kept the real path untouched: extract the subscribe+fire-and-forget-start+`.finally` cleanup into one private `launch(driver, seed)` typed by a local `RunDriver` interface; `start()` (real) and `startSimulation()` (synthetic) both call it. The whole web kanban/log/drawer renders ENTIRELY from `run.state` (agents[]→agent cards, stageIntegrations[]→merge cards, checkRuns[]→judge cards; columns from `phase`; ↻ from `requeues`; tokens/cost from agent fields) — any producer of valid OrchestratorState drives the full UI without a real run, LLM, key or git worktree.
- [2026-06-26][source:inference][task:simulation-mode][probation] Serving the SPA at a SECOND path (`/simulation`) needs an EXPLICIT `serveStatic(res, root, 'index.html')` route placed BEFORE the catch-all `GET !startsWith('/api/')` static handler — otherwise the catch-all does `serveStatic(root, 'simulation')` and 404s on the bare path. The client then routes on `location.pathname.replace(/\/+$/,'')==='/simulation'` inside `boot()` and short-circuits the launch flow (`bootSimulation(b); return;`). New no-key endpoints added the same way: `POST /api/run {simulate:true,modelIds,fileCount,concurrency}` (branches at the TOP of `startRun` before any backend/provider/key resolution) and `POST /api/run/pause {paused}`.
- [2026-06-26][source:inference][task:simulation-mode][probation] Make a state-machine engine testable WITHOUT timers/wall-clock: split one logical step into a public `advance()` and have `start()` merely `setInterval(()=>advance(), tickMs)`. Tests drive `advance()` in a loop to completion (cap iterations so a regression can't hang) and assert invariants every tick (e.g. `activeAgentCount <= concurrency`). Progression uses an `activeTicks` counter (frozen while paused) for `elapsedMs`, NOT `Date.now()`, so assertions are deterministic; a seeded mulberry32 PRNG makes scenario sampling reproducible (same seed ⇒ identical agent-phase sequence). `start()` only needs to resolve `{runId, manifest:{errorReason?}}` — the sole fields the manager reads — so a minimal `SimulationResult` satisfies the `OrchestratorResult` driver contract.
- [2026-06-26][source:inference][task:kanban-scroll-and-move-flip][probation] Kanban columns squashed cards even though `.lane__body` already had `overflow:auto` — the real cause was `.card` (a flex child of the `flex-direction:column` body) keeping the default `flex-shrink:1`, so flexbox compressed every card to fit BEFORE any scrollbar appeared (and `.card{overflow:hidden}` then clipped the content = "achatado"). Fix is one line: `.card{flex:0 0 auto}` (never shrink) + make `.lane__body` the bounded scroll container with `flex:1 1 auto; min-height:0; overflow-y:auto` (use `flex-basis:auto`, NOT `flex:1`/basis-0 — basis-0 can collapse the body to 0 height in the mobile single-column `.board{overflow:auto}` layout). Lesson: `overflow:auto` on a flex container is a no-op until its flex children are told not to shrink.
- [2026-06-26][source:inference][task:kanban-scroll-and-move-flip][probation] FLIP card-move animation across kanban columns (transform-only, GPU-composited): the lane bodies clip (`overflow-y:auto` forces overflow-x to `auto` too — CSS can't mix `visible` with a non-visible axis), so a card translated toward another column gets clipped mid-flight. Solution = animate a `position:fixed` GHOST (a `cloneNode(true)`) on a body-level `.flip-layer{position:fixed;inset:0;pointer-events:none}` (NOT inside `.board`/any backdrop-filter/transform ancestor, else fixed is contained & clipped), hide the real card with `visibility:hidden` to hold its destination slot, then translate the ghost old→new and swap back on `transitionend` (+ a `setTimeout(dur+140)` fallback for backgrounded tabs). Batch the FLIP: read all First rects, mutate DOM, read all Last rects, write all inverts, ONE `void document.body.offsetWidth` reflow, ONE rAF to play — zero per-card layout thrash. Gate the whole thing on `!matchMedia('(prefers-reduced-motion: reduce)').matches`, board visible, and a structural-change check (only measure when a card was added/removed/moved) so idle SSE frames stay cheap.
- [2026-06-26][source:inference][task:kanban-scroll-and-move-flip][probation] Board cards are REUSED dom nodes keyed by `card.key` (`cardEls` Map) where keys (`a1`,`a2`,`m0`,`j0`…) REPEAT across runs — so any per-card animation state (last-lane / sort-rank maps) MUST be wiped when the run changes, else a fresh run's pending cards read as movers and fly in from a previous run's lanes. Detect via `run.runId` (present on the snapshot, already used by the queue) changing, and full-reset before `renderBoard`. "Card lands at the destination's FIRST slot" is just ordering: give a lane-changer a descending rank (newest mover smallest) and new cards an ascending rank, then sort each lane by rank — movers stack on top, naturals keep insertion order below.
- [2026-06-26][source:inference][task:kanban-scroll-and-move-flip][probation] You CAN ship a COMMITTED unit test for vanilla client logic despite `tsconfig allowJs:false` + no client harness: extract the pure logic into a `client/<name>.js` ES module, then add a `<name>.test.js` (NOT `.test.ts`). vitest's default `include` matches `[jt]s` so it RUNS the `.test.js`, while `tsc --noEmit` IGNORES all `.js` (no allowJs) so `npm run typecheck` stays green with zero `.d.ts` ceremony. Place the test OUTSIDE `client/` (e.g. `src/web/<name>.test.js`) so the build's `cp -R src/web/client/.` doesn't copy it into the served/shipped bundle (the `.js` MODULE stays in `client/` — it must be served raw + copied to dist). This upgrades the skill's "verify with no browser" from ad-hoc Node scripts to a real regression gate for extractable client logic.
- [2026-06-26][source:user][task:open-app-landing-and-disconnect][probation] The web client's INITIAL view is NOT decided in `boot()` — it falls out of `ingestRun(run)`, which runs both at boot (`ingestRun(b.run)`, `b.run` = `serializeSnapshot(manager.getSnapshot())` from `/api/bootstrap`) AND on every `/events` `run` frame. User wants "open on HOME unless a pipeline is RUNNING; if running, open on the board". Fix was one gate: the auto-switch was `if (hasRun && $('viewRun').hidden) showView('run')` where `hasRun = phase!=='idle'` — so a done/error run on a fresh load hijacked the board. Change to `if (active && …)` where `active = phase==='running'`. Key property preserved BECAUSE the guard only ever switches TO the board (never away): a run that settles to done WHILE you watch keeps you on the board (active→false just stops re-asserting it, the view is already shown). Also tie the topbar run chrome to board context so home stays clean on reopen-after-done: compute `onRunView = !$('viewRun').hidden` AFTER the switch, then `$('runStatusGroup').hidden = !hasRun || !onRunView` (don't gate it on `active` alone or the status pill vanishes the instant a watched run finishes). `concControl`/`abortBtn` already gate on `active`; `backToLaunch` lives inside the run-view footer so its `active || !hasRun` gate is board-only. Server contract that drives the decision is already test-covered: bootstrap reports `idle` fresh (existing test) and `running` mid-run (new test) — the client gate itself is untestable here (no DOM harness), verify by reasoning + `node --check app.js`.
- [2026-06-26][source:user][task:open-app-landing-and-disconnect][probation] "Closing the site must not interrupt the run" is ALREADY guaranteed by the architecture — the run is server-side and fire-and-forget. `WebRunManager.launch` calls `driver.start().then/.catch/.finally` with NO tie to any browser; the SSE `req.on('close')` handler (server.ts) ONLY does `clearInterval(ping)` + `sseClients.delete(client)` — it NEVER aborts. The sole teardown-abort is `server.on('close')` = the huu PROCESS stopping (Ctrl+C), not a tab closing. There are ZERO client unload/`beforeunload`/`pagehide`/`sendBeacon` handlers (grep to confirm before adding any — one would silently break this), and `/api/run/abort` fires only from the explicit Stop / Stop-queue buttons. On reopen the browser re-syncs via `/api/bootstrap` + the SSE `lastFrame` replay. Locked with a NAMED regression test in `server.test.ts` ("keeps the run alive when the browser (SSE) disconnects"): POST a stub run → `isActive()` true → open `/events`, read one frame, `reader.cancel()`, 150ms → assert `isActive()` STILL true AND `/api/bootstrap` `run.phase==='running'`. Non-flaky because stub agents sleep 2–5s (`backends/stub/factory.ts`), far longer than the disconnect window.
- [2026-06-26][source:inference][task:default-model-deepseek-v4-flash][probation] The web picker's "default" `if (!S.modelId && S.models.length) S.modelId = S.models[0].id` (`refreshModelsAndKeys`, `app.js`) is NOT a curated default — for `pi` the live catalog is sorted ALPHABETICALLY by id (`projectAllModels` in `lib/openrouter.ts` does `out.sort((a,b)=>a.id.localeCompare(b.id))`), so `models[0]` is just whatever id sorts first, NOT the recommended headline. To honor a real default model, preselect `DEFAULT_MODEL_ID` when the loaded catalog contains it, else fall back to `models[0]`. The id had to be DUPLICATED as a `const DEFAULT_MODEL_ID` at the top of `app.js` (vanilla JS, no build → can't import the TS `catalog.ts` constant); flag the mirror in a comment so they stay in sync. `node --check app.js` is the only gate (client `.js` isn't type-checked/tested).
- [2026-06-26][source:inference][task:default-model-deepseek-v4-flash][probation] PRE-EXISTING broken test on `development`, NOT introduced by this task (proven by `git stash` → still red): `server.test.ts` > "keeps the run alive when the browser (SSE) disconnects" reads `boot.run.phase` but the multi-run merge (`feat(web): run multiple projects in parallel`) changed `bootstrapPayload()` (server.ts) from `{run: …}` to `{runs: manager.getSnapshots().map(serializeSnapshot)}`. So `boot.run` is `undefined` → `Cannot read properties of undefined (reading 'phase')`. This SUPERSEDES the assertion detail in the `[task:open-app-landing-and-disconnect]` learning above (which still says the test asserts `/api/bootstrap` `run.phase==='running'`). Fix when in scope: `expect(boot.runs[0].phase).toBe('running')`. Lesson: a server-shape change (`run`→`runs[]`) from a parallel branch can silently strand a NAMED regression test that another branch authored against the old shape — grep `bootstrap` consumers in tests after any bootstrap-payload edit.
- [2026-06-28][source:user][task:web-project-selector-dropdown][probation] User wanted the multi-run HEADER selector to (a) show the PROJECT name + pipeline, and (b) be a `<select>` dropdown instead of side-by-side buttons. Two-part fix. SERVER: `RunSnapshot` carried only `pipelineName` — there was no per-run project identity — so added `runDirectory: string` to the interface + `IDLE_SNAPSHOT`, set it in `start()` (= the resolved `runDir`) and `startSimulation()` (= `this.cwd`), and exposed it in `server.ts` `serializeSnapshot` (the ONLY shape the client sees; the SSE `run` frame + `/api/bootstrap runs[]` both flow through it). CLIENT (`app.js`): `renderRunSelector()` now emits `<span class="run-select__dot" data-phase>` + `<select id="runSelect">` whose options read `projectName(runDirectory) · pipelineName` (+ ` ✓`/` ✕` suffix for done/error); `projectName(p)` = last path segment. The handler switched from click-delegation on `.run-tab` to a `change`-delegation on the stable `#runSelector` container (the `<select>` is rebuilt every render, so delegate the BUBBLING change event; check `e.target.id === 'runSelect'`). This SUPERSEDES the SKILL.md body's "`.run-tab`" reference (now `.run-select`/`#runSelect`). Native `<select>` can't color per-option, so the active run's phase shows via the animated leading dot (reuses the `pulse` keyframe) and finished runs get a static ✓/✕ in their option text. Server change is test-covered (`server.test.ts` asserts `j1.run.runDirectory === repo` + the settled snapshot's `runDirectory`; `run-manager.test.ts` asserts the sim reports `process.cwd()`); the client `<select>` itself is untestable here — `node --check app.js` + boot `--web --stub` + curl `/styles.css|/app.js` for `run-select`/`runSelect` is the gate.
- [2026-06-26][source:user][task:realtime-cost-and-log-fixes][probation] Three user-reported web-dashboard bugs fixed together. (1) HEADER COST never summed: `Orchestrator.getState()` returned `totalCost: 0 // M5 will populate` and the header reads `st.totalCost`, so it sat at $0.00 even though per-agent `AgentStatus.cost` accumulates fine. Fix: getState now computes `totalCost = +Σ agents.cost .toFixed(4)` (mirrors `SimulationEngine.getState`, which already did this). This CORRECTS the earlier [task:sequential-projects-queue] entry's claim that merge/judge cost "folds into totalCost" — it does NOT: merge(9999)/judge(9998) agents aren't in `this.agents`, so their LLM cost is metered NOWHERE (totalCost = Σ agent-card cost only; future work must capture their usage events to count them). (2) Header used `(st.totalCost||0).toFixed(2)` → sub-cent OpenRouter costs render "$0.00" and look unmetered; added `fmtCost(n)` (4 decimals under $1, 2 at/above) — use it for any live cost readout. (3) LOG DRAWER auto-scrolled to the bottom on EVERY 120ms snapshot because `refreshDrawer` called `scrollLogs()` unconditionally — a reader who scrolled up was yanked back. Fix: `setDrawerLogs(text, force)` measures pinned-to-bottom (within `STICK_THRESHOLD_PX=28`) BEFORE replacing `textContent`, then follows the tail only if pinned (or `force` on first open), else restores `prevTop`; bails when text is unchanged (no needless scroll reset). `openDrawer` clears `#drawerLogs` first so the first render starts pinned.
- [2026-06-29][source:user][task:web-project-selector-motion-dropdown][probation] The multi-run header project selector was a native `<select id="runSelect">` — it "opens and immediately closes" during a run because `renderRunSelector()` rebuilds the container via `el.innerHTML` on EVERY SSE `run` snapshot (~8×/s, throttled to 120ms), and recreating a `<select>` slams the OS dropdown shut the instant it opens. ROOT CAUSE is the rebuild-on-render, not the control. SUPERSEDES the `[task:web-project-selector-dropdown]` entry (the `<select id="runSelect">` + change-delegation it introduced is GONE). Fix == redesign: a custom simulated listbox (`.rsel__trigger` pill + `.rsel__menu` glass `<ul role=listbox>`, ids `runSelTrigger/runSelMenu/runSelLabel/runSelChev`). The fix is structural — open state lives in a module `const runSel={open,active}` over PERSISTENT DOM whose listeners are wired ONCE by `ensureRunSelDom(el)` (guarded `if ($('runSelTrigger')) return;`); `renderRunSelector()` then only updates the trigger label/dot and, WHILE OPEN, calls `renderRunSelMenu()` to refresh the `<li>` rows — it NEVER touches `runSel.open`/`menu.hidden`, so the menu survives the re-render firehose. Keyboard all on the trigger (it keeps focus; menu is presentation): ArrowUp/Down move `runSel.active`, Enter picks, Escape closes. Outside-dismiss = one `document` `mousedown` listener (`!host.contains(e.target)`); option pick = `menu` `mousedown` (beats focus/blur). `.run-selector` needed `position:relative` for the absolute menu. `node --check app.js` + boot `--web --stub` + curl is the gate (client `.js` untyped/untested); the >1-run condition means the selector only shows with 2+ concurrent runs.
- [2026-06-29][source:user][task:web-project-selector-motion-dropdown][probation] Using Motion (motion.dev) in the no-build, no-CDN, offline vanilla-ESM client: VENDOR `node_modules/motion/dist/motion.js` (the UMD production build, ~139KB, v12.42.0) into `src/web/client/vendor/motion.js` and load it via a CLASSIC `<script src="vendor/motion.js">` placed BEFORE the deferred `<script type="module" src="app.js">` — classic non-deferred scripts run before deferred modules, so `window.Motion` (`.animate/.spring/.stagger/.hover/.press`) is ready when app.js runs. The build's `cp -R src/web/client/. dist/` ships it (and subdirs serve fine: `serveStatic` does `join(root, relPath)`, `.js`→text/javascript). Do NOT use the package's ESM entry `dist/es/index.mjs` — it `export * from 'framer-motion/dom'`, i.e. NOT self-contained, unusable without a bundler. App code reads `window.Motion` LAZILY via a `motionEngine()` helper and DEGRADES gracefully (no animation, just toggle `hidden`) when absent or under `prefers-reduced-motion` (`runSelMotion()` returns null). Dropdown anim recipe: open = `M.animate(menu,{opacity:[0,1],scale:[.96,1],y:[-6,0]},{type:'spring',stiffness:520,damping:32})` + option `M.stagger(0.025)` + chevron `rotate:180`; close animates out then hides on `anim.finished` ONLY `if (!runSel.open)` (a re-open mid-exit must win; `.catch` the promise too). VERIFY GOTCHA: `require()`-ing the UMD under huu's `"type":"module"` package scope parses it as ESM → empty namespace (all exports undefined) while the GLOBAL branch fires instead — verify the API by copying to a `.cjs` (outside the package scope) and requiring that (`Object.keys(M).length===312`), or by vm-eval'ing with no `exports/module/define`. NOTE: `motion-plus` (premium catalog) is React-only + served from a token-gated registry (npmjs ships a frozen 1.5.1 tombstone) — it does NOT apply to a vanilla client; "Motion+" here means Motion's core `animate`/`spring`, the same engine. See `vendor/README.md` for the update procedure.
- [2026-06-29][source:inference][task:run-log-redesign][probation] A cross-project "N tasks running NOW across ALL runs" live counter needs ZERO server/orchestrator/SimulationEngine change: `server.ts serializeSnapshot` sends `state: trimState(state)` and `trimState` does `{...state}` (only caps per-AGENT logs), so the FULL `OrchestratorState` — incl. `activeAgentCount` (= `this.activeAgents.size`, set in `getState`), `pendingTaskCount`, `concurrency` — already reaches the client inside `run.state`, even though the UI never displayed them. The client already holds EVERY concurrent run in `S.runs` (Map<runId,snapshot>), so the global aggregate is pure client math: `Σ run.state.activeAgentCount` over `[...S.runs.values()].filter(r=>r.phase==='running')` (+ `Σ pendingTaskCount`, + project count). Don't add a server field for something getState already computes and trimState already passes through — grep `serializeSnapshot`/`trimState` first to see what's already on the wire.
- [2026-06-29][source:inference][task:run-log-redesign][probation] A GLOBAL (all-runs) readout must render from `renderActiveRun` but OUTSIDE its `if (st) {…}` guard. `renderActiveRun` already runs on EVERY run's frame (it's called from `ingestRun`, which fires per-`runId` for every concurrent run), so it's the right hook for a live cross-run aggregate — BUT the `if (st)` block only executes when the SELECTED/viewed run has `state`, so a global counter left inside it FREEZES whenever the viewed run lags behind another live run (or the viewed run is idle/done while others run). Put the per-run board rendering inside `if (st)` and the global render (`renderLog()` here, which owns both the activity counter and the log body) at the END of `renderActiveRun`, unconditionally. The old `renderLog(st.logs)` call site was per-viewed-run; making it argument-less + reading `S.runs` itself is what makes it global.
- [2026-06-29][source:user][task:run-log-redesign][probation] Run-log redesign decisions that shipped (web `.logbar`): the BOARD stays per-selected-run (project selector switches it) but the LOG went GLOBAL — when >1 run is live the body is ONE timestamp-sorted stream merging every `S.runs[*].state.logs`, each line tagged with `projectName(runDirectory)`; a single run renders untagged. Per-agent visual identity = a stable golden-angle hue `Math.round((agentId*137.508)%360)` emitted as an inline `style="--ah:<h>"` and consumed by CSS `hsl(var(--ah) …)` with a DARK/auto lightness override (mid-lightness saturated colors need a brighter variant on dark glass, set under `:root[data-theme='dark']` AND `@media(prefers-color-scheme:dark) :root[data-theme='auto']`). Reserved ids get SEMANTIC chips, not hues: `agentId<0`→ORQ, `9999`→INT(◆), `9998`→JDG. The header doubles as the live activity bar (it never collapses, so the counter is always visible); level filter chips + a "↓ Latest" jump pill (absolute-positioned on `position:relative .logbar`, toggled by a `scrollHeight-scrollTop-clientHeight<48` bottom check) + auto-expand-once-on-first-live (latched by `S.logAutoExpanded`, suppressed once `S.logUserToggled`). Counter placement (header vs topbar pill) was the user's call → header, so the global number is labeled "all projects" and never mixes with the topbar's per-VIEWED-run metrics.
- [2026-06-29][source:user][task:add-pipelines-in-execution][promoted] User wanted to return HOME during a live queue, add more projects, and have them processed automatically (no prompt) with a "running" signal visible while selecting. The SERVER half was ALREADY complete + test-covered: `WebRunManager` holds `Map<runId>` over one `GlobalScheduler`, each `/api/run` does `scheduler.register()` (re-grants live, backfill by priority) with NO 409 (429 only past `MAX_CONCURRENT_RUNS=24`) — so a run added mid-flight is admitted + scheduled with zero server change. The whole feature was 4 CLIENT control-flow edits in `app.js` + markup/css: (1) a sticky `S.homePinned` flag — the load-bearing piece, because `renderActiveRun`'s board auto-switch `if (active && $('viewRun').hidden) showView('run')` re-asserts the board on EVERY SSE frame (see [task:open-app-landing-and-disconnect]), so `showView('launch')` alone is yanked back next frame; gate it `&& !S.homePinned`, set the flag in the `backToLaunch` click when `q.running`, clear it in `startQueue`/`finishQueue`/`stopFinalize`/the "View board →" button. (2) MOVE the per-dispatch `showView('run')` OUT of `postRun` INTO `startQueue` — else an incremental add jumps you to the board. (3) form-submit: when `q.running`, `dispatchQueueItem(items.length-1)` immediately instead of just queueing (the "Add & start" path). (4) `updateQueueChrome` now SHOWS `backToLaunch` ("← Home") during a queue (was hard-hidden "no wandering off") + a launch-view banner `#launchRunning` ("View board →"). Queue-completion target `q.settled >= q.items.length` is read LIVE, so appended items extend it with NO finish-logic change — the only edge is the drain-race (all current items settle in the gap before the next add → `q.running` flips false, banner hides, normal "Run queue" resumes); left as acceptable. Pure logic extracted to `client/queue-util.js summarizeQueue()` (+ `.test.js`).
- [2026-06-29][source:inference][task:add-pipelines-in-execution][probation] Running the vitest/tsc gate inside a git WORKTREE that has no `node_modules`: symlinking `node_modules` → the main worktree's (same base commit) works for `./node_modules/.bin/vitest` + `tsc`, BUT `.gitignore`'s `node_modules/` (trailing slash = directory only) does NOT match a SYMLINK named node_modules, so `git status` shows `?? node_modules` — remove the symlink before finishing or it reads as a stray untracked entry. Also: the globally-installed `npx vitest` can be broken (missing `@rolldown/binding-*` native module); always use the project-local binary.
- [2026-06-28][source:user][task:dynamic-filenames-board-titles][probation] Web card titles are built from the step NAME (`a.stageName` / `m.stageName` / `j.stepName`), and per-file/`memory` steps are NAMED with the literal `$file` fan-out token (e.g. `"3. Write tests for $file"`) — so the raw `$file` leaked into the live board (`agentCard`/`mergeCard`/`judgeCard` in `app.js`), the drawer Stage field, AND run history (`db.js buildHistoryCards`). Fix: a pure DOM-free `client/title-util.js` (`substituteFileInTitle(title,file)` → basename of `currentFile`, or `"files"` when falsy) MIRRORING `src/lib/title-format.ts` (the two-worlds pattern: vanilla-ESM client can't import the typed lib; sibling `.test.js` in `src/web/` is the Node-testable spec — same as board-order/queue-util). Compute the card's `file` ONCE and pass it in; merge/judge cards pass null. Display-only — the agent prompt keeps the exact path. Server needs ZERO change (it just passes `OrchestratorState` through; the client owns card building).
- [2026-06-29][source:user][task:timeout-error-retry][promoted] Failed-card retry + distinct timeout signalling in the web UI. (1) The client NEVER read `errorKind` — `agentCard` (`app.js`) only branched on `phase==="error"` → red `.phase.err`. Now an `errorKind==="timeout"` agent card gets `cls="tmo"` + label "timeout" → a new amber `.phase.tmo` (`--yellow`), distinct from red "failed"; a `manualRetries` foot bit (`retry N`) mirrors the requeue `↻N`. (2) The KEY gotcha for the held-open run: while in `awaiting_retry` the run-manager has NOT settled, so the `RunSnapshot.phase` is STILL `running` — drive ALL retry/finish chrome off the INNER `run.state.status==="awaiting_retry"`, NOT `run.phase`. `setStatus(phase, innerStatus)` shows an amber "review" pill (`[data-s="awaiting"]`); `#finishBtn` (topbar) shows only when awaiting. (3) Drawer retry: `renderDrawerRetry(c, st)` renders a `#drawerRetry` block ONLY for an agent card in error AND `st.status==="awaiting_retry"` — a timeout card adds a minutes `<input>`, any error a Retry button; delegate the click from the STABLE `#drawerRetry` container (innerHTML is rebuilt each `refreshDrawer`). New routes `POST /api/run/retry {runId,agentId,timeoutMinutes?}` (400 on missing/non-numeric agentId; 200 no-op for unknown run) and `POST /api/run/finish {runId}` forward to `WebRunManager.retryTask`/`finish` (real runs only; `orch.retryTask`/`finish`). `interactiveRetry:true` is set on EVERY web Orchestrator (even subordinate/multi-run: a held-open run has `getDemand()===0` so it never starves the scheduler; abort-all still tears everything down). The stub backend never fails, so web e2e of the hold-open path is NOT unit-testable — coverage = route validation tests + orchestrator-level `retry-task.test.ts`; gate the client with `node --check app.js` + boot `--web --stub` + curl for `finishBtn`/`drawerRetry`.
- [2026-06-29][source:user][task:web-agent-timeout-launch-field][probation] Adding a per-project "max time per agent" control to the web LAUNCH form (user-requested) was CLIENT-ONLY — the server already accepted it end-to-end: `StartRunParams.timeoutMinutes` → `server.ts startRun` reads `body.timeoutMinutes` → `run-manager.ts start()` calls `applyTimeout(pipeline, minutes)` which sets BOTH `cardTimeoutMs` + `singleFileCardTimeoutMs` (the whole-pipeline per-agent cap, same as the TUI TimeoutPrompt). The "configurable timeout" the web had before was ONLY the reactive retry-a-timed-out-card field (`[task:timeout-error-retry]`); there was NO proactive launch field — that was TUI-only (PipelineEditor). So the feature = a number input `#timeoutInput` → `parseTimeoutMinutes()` (new pure helper in `client/queue-util.js`, unit-tested) → the captured queue-item config (`captureFormConfig`) → the `postRun` body (`timeoutMinutes`). Reused lesson: grep the server param shape (`StartRunParams` + the `startRun` body parse) BEFORE assuming a missing UI needs a server change. Two durable client gotchas: (1) the generic input rule `select, input[type='password'], input[type='text']` does NOT match `input[type='number']`, so a number field needs its OWN box style — but focus accent IS shared (the `select:focus, input:focus` rule is unqualified); (2) `persistQueue`/`restoreQueue` and `db.js` history records build from the queue ITEM by spread/field-copy, so a NEW per-project config field survives reload + lands in History with ZERO persistence change — just add it to the captured item (and, for History, one line in `buildRunRecord`/`buildSyntheticRecord`). Made `applyTimeout` EXPORTED to pin it with a unit test (`run-manager.test.ts`), mirroring the existing `applyResolverModel` test (start-path timeout had zero coverage before).
- [2026-06-29][source:user][task:web-agent-timeout-launch-field][probation] FOLLOW-UP: user wanted the timeout as a GLOBAL Web-UI config, NOT only per-project. Added the FIRST real "Settings" surface to the web client — a ⚙ topbar button + a modal that MIRRORS the history modal (`#settingsScrim` + `.settings-modal.glass` + reused `.folder-modal__head`/`.field`/`.timeout-*`; open/close = unhide scrim+modal, Esc + scrim-click close) holding ONE global "Max time per agent" persisted under `localStorage 'huu.settings.v1'` (`{maxAgentMinutes}` — the first browser-owned PREFERENCE object beyond `huu.theme`/`huu.queue.v1`). Resolution model the user picked (global default + per-project OVERRIDE): `postRun` sends `item.timeoutMinutes || globalTimeoutMinutes() || undefined`; `syncTimeoutField()` shows the global as the per-project field's PLACEHOLDER (blank inherits); queued cards render the EFFECTIVE `override ?? global` timeout (so `renderQueue` re-runs when the global changes). Scope rule the user stated explicitly: this is WEB-UI-ONLY — the CLI/TUI keeps its own timeout rules (PipelineEditor/TimeoutPrompt), untouched (the web change never imports/touches `src/ui`). Reusable recipe for a NEW web preference: add to `S.settings`, `load/saveSettings` over `huu.settings.v1`, call `loadSettings()` on boot right after `restoreQueue()`, and reflect into any dependent field.
- [2026-07-01][source:user][task:multi-run-list-order-priority][promoted] REFINES [task:fase1-web-lazy-admission]'s "the browser keeps POSTing every item (forEach), the server paces them": `startQueue`'s `q.items.forEach((_,i)=>dispatchQueueItem(i))` dispatched all `/api/run` POSTs CONCURRENTLY, so the server received them (and `pending.push()` = admission/priority order) in a RACE across the browser's ~6 parallel connections — the FIRST project in the list was NOT guaranteed highest priority, even though the code comment claimed "priority = dispatch order". FIX (client, ordering): `startQueue` is now `async` and `await`s each `dispatchQueueItem(i)` (which now RETURNS `postRun(i)`'s promise) in a `for` loop — the server enqueues each run BEFORE responding, so awaiting the response guarantees list-order arrival; `postRun` swallows its own errors so one bad project can't stall the loop. FIX (server, transport-independent belt to that suspenders): `postRun` body now sends `priority: i` (the queue index) → `server.ts` parses `body.priority` → `StartRunParams.priority` → `WebRunManager.start` passes it to `OrchestratorOptions.priority` AND keeps the `pending` array sorted by priority (`pending.push({…,priority}); pending.sort((a,b)=>a.priority-b.priority)`) so `admitNext()`'s `shift()` always pulls the earliest-list project even if two POSTs reorder; absent priority → a monotonic `this.enqueueSeq++` fallback. Mid-run "Add & start" (`dispatchQueueItem(items.length-1)`) carries its index too (reorder/remove are DISABLED while `q.running`, so `q.items` indices are stable during a live queue). The lazy-admission PACING (500ms loop + `AdmissionController`) is UNCHANGED — only dispatch ORDER + authoritative priority were added. Client is untestable here (`node --check app.js` gate); the server-side priority guarantee is pinned in `global-scheduler.test.ts` (working-on-orchestrator domain, `[task:multi-run-list-order-priority]`).
- [2026-06-30][source:user][task:fase1-web-lazy-admission][promoted] The web fired the WHOLE project queue at once (`app.js startQueue` → `forEach dispatchQueueItem`, each an immediate `POST /api/run`) while only headless `run-many` had lazy admission — the direct cause of a 9-run OOM. FIX = SERVER-SIDE admission in `WebRunManager`, NOT client throttling (the browser keeps POSTing every item; the server paces them — browser-owns-state stays true for the queue DISPLAY, server owns ADMISSION). The decision was extracted to a shared leaf `lib/admission-controller.ts` (`AdmissionController.shouldAdmit({liveAdmitted,pendingCount,schedulerRemaining,anyIntegrating})` + internal hysteresis streak; `run-many` now uses it too). `start()` no longer launches immediately: it builds the Orchestrator (cheap — `scheduler.register` is in `orch.start()` not the ctor, so a QUEUED orch consumes ZERO budget), registers a `phase:'queued'` entry in `runs`, pushes `{runId,orch,seed}` to `pending`, and a 500 ms `setInterval` admits via the controller + `scheduler.remaining`; the FIRST run (activeCount 0) admits immediately (mirrors run-many). `launch()` was split into `launch` (sims = immediate) + `beginRun` (shared subscribe/start/settle on an EXISTING entry); `admitNext()` flips queued→running then `beginRun`. NEW `RunPhase 'queued'`; `MAX_CONCURRENT_RUNS` 24→64 is now the cap on TOTAL non-terminal (queued+running), live concurrency capped by `MAX_LIVE_RUNS=8`; `isActive()`+new `nonTerminalCount()` count queued; `abort()` drains `pending` + stops the loop. CLIENT just renders the phase: `statusBadge`/status-pill/run-select-dot + CSS (`--yellow`), `dispatchQueueItem` sets item 'queued', `onRunFrame` syncs queued→running. Also added the machine-global RAM dial as the web's SECOND prefs key (`huu.settings.v1.ramPercent`) → `POST /api/run` `ramPercent` → `StartRunParams` → `scheduler.setBudgetPercent(resolveRamPercent(params.ramPercent))` — NO per-project override (one machine, one RAM), unlike the per-project max-time field. Pinned by run-manager.test.ts (stub: first run 'running', second 'queued', both drain — needs an explicit 30s `it` timeout, it exceeds vitest's 5s default under suite parallelism).
- [2026-07-01][source:user][task:web-guided-launch-wizard][promoted] User: the launch flow was "bad" — wanted pick-pipeline → mark MANY project folders → optionally add ANOTHER pipeline → all into the queue (a "cart"). Rebuilt `#viewLaunch` into a 4-step wizard (`#stepper` chips + `#step1..4`, toggled by `goStep(n)`; chips are clickable, gated by `canGoStep(n)`). Whole thing was CLIENT-ONLY over the EXISTING per-item POST path — REUSED [task:multi-run-list-order-priority]'s `startQueue → dispatchQueueItem → postRun(priority:i)` and [task:fase1-web-lazy-admission]'s server pacing UNCHANGED. The core primitive: a pipeline batch (one config) FANS OUT over its marked folders into N flat `S.queue.items`, one run per folder. Pure fan-out/group logic went in `client/queue-util.js` (`fanOutBatch(base,dirs,groupId,mkId)` = `dirs.map(d=>({...base,id:mkId(),runDirectory:d,groupId,status:'pending'}))`; `groupQueueItems(items)` = order-preserving groups keyed by `queueGroupKey`=`groupId ?? 'item:'+id` so LEGACY groupId-less items each singleton). Config is PER-PIPELINE (shared by its projects), decided with the user. `renderQueue` now renders GROUPED (`.queue-group__head` + rows), rows lead with `projectName(runDirectory)` (pipeline id is in the header), and `renderQueue` calls `renderStepper()` each frame — SAFE because it only touches `#stepper`, never the wizard input panels (SSE never re-renders steps 1–3). De-scoped inline edit + per-item reorder (batch model doesn't map cleanly); kept per-item + NEW per-GROUP remove. `commitBatch()` "spends" the batch (`S.selectedPipe=null; markedDirs.clear()`) so a chip can't double-add. Folder picker (step 2) is a NEW multi-select over the SAME `/api/folders` nav: `folderRow` = a `.folder-check` button (toggles `S.markedDirs` — absolute paths, PERSIST across nav) + a `.folder-item__label` (sub-folder = navigate IN; the sticky self-row `folder-item--self` = mark THIS dir, whose git status is the only one `/api/folders` reports). No server run-path change; verified `node --check app.js` + boot `--web --stub` + curl (wizard markup present, old `#folderModal`/`#dirBrowse`/`#configEmpty` gone) + a stub e2e POST reaching `done`.
- [2026-07-01][source:inference][task:web-guided-launch-wizard][probation] CORRECTS the SKILL body's "there is **no client test harness**" for PURE modules: vitest's DEFAULT `include` glob is `**/*.{test,spec}.?(c|m)[jt]s?(x)` — the `[jt]s` matches `.js`, so a colocated `src/web/client/queue-util.test.js` (which never existed despite `queue-util.js`'s header comment referencing it) IS picked up and run by `npm test` (esbuild transforms it, so `tsconfig`'s missing `allowJs` is irrelevant — that only gates `tsc` typecheck, not vitest). So DOM-free client modules (`queue-util.js`, `board-order.js`, `title-util.js`, `db.js` builders) are unit-testable IN PLACE with a `.test.js`, not only via a separate Node import script. The "no harness" caveat is real ONLY for code that touches `document`/DOM at import or call time.
- [2026-07-01][source:inference][task:web-guided-launch-wizard][probation] `api-data.ts listDirs` (the `/api/folders` picker source) filtered `e.isDirectory() || e.isSymbolicLink()` — so a symlink to a FILE (e.g. this repo's `CLAUDE.md -> AGENTS.md`) showed up as a navigable/markable "folder". Harmless with the old single-select "Run here" (you'd never navigate into a file), but the new checkbox picker makes it MARKABLE as a bogus project. Fix: for symlinks, follow the link with `statSync(join(path,e.name)).isDirectory()` inside a try (broken links → excluded), keeping dir-symlinks and dropping file-symlinks. First-ever `listDirs` test added to `api-data.test.ts` (real dirs + real symlinks in `mkdtemp`, no fs mocks — the repo convention). Lesson: a UI that turns a previously navigate-only listing into a SELECTABLE one re-surfaces every leniency in that listing's server filter — re-audit the data source, don't just build the widget.
- [2026-07-02][source:user][task:web-live-settings-budget-telemetry][promoted] The RAM dial "never took" class of bug: a machine-global setting that only travels PIGGYBACKED on `POST /api/run` (a) silently does nothing when changed mid-run (no further POSTs), (b) silently reverts to the default when the field is blank at POST time (`x || undefined` → server default 85), and (c) has zero feedback either way. PATTERN for any machine-global web setting: server-owned `POST /api/settings` that applies IMMEDIATELY (here `manager.setRamPercent` → `scheduler.setBudgetPercent`), persists server-side (`src/lib/web-settings.ts`, XDG `~/.config/huu/web-settings.json`, load-on-boot), and ECHOES the effective value; `/api/bootstrap` carries `settings` back so the ⚙ modal shows the value in force; localStorage is demoted to a display cache. Client posts on `change` (commit), keeps `input` for local cache. Run POSTs no longer carry `ramPercent` (server ignores it — pinned by a server.test.ts regression). OBSERVABILITY twin: a 1 Hz `{type:'budget'}` SSE frame (own low-freq frame type per the high-frequency rule; short-circuits with 0 clients) feeds a topbar `#budgetChip` (dial %, used/total GiB, pressure label via `data-level` tokens) from `GlobalScheduler.budgetTelemetry()`. A grant-starved run (running, 0 active agents, pending > 0, guard level ≥ 1) is surfaced as an amber pulsing `paused (RAM)` pill DERIVED client-side in `setStatus` — no new server state machine; it resumes in place. Settings tests MUST set `XDG_CONFIG_HOME` to a mkdtemp dir around `createWebServer` or they write the user's real config (WebRunManager loads settings at CONSTRUCTION). Admission hardening: `anyIntegrating` now only short-circuits the hysteresis WHEN capacity is positive (it used to admit regardless — the hole that pulled runs in while the guard was shedding); per-run baseline (`HUU_RUN_BASELINE_MB` 384) charged against `scheduler.headroomBytes()`; live cap adapts (budget ÷ (baseline + charge), ceiling `HUU_MAX_LIVE_RUNS` 8); accept cap is `HUU_MAX_QUEUED_RUNS` (256) — queued Orchestrators cost no budget (registration happens in start() at admission), so a deep queue is the SUPPORTED path for "run all my projects". Terminal runs get a slim snapshot + retention cap 100 (the Map previously kept full states forever).
- [2026-07-02][source:user][task:folder-picker-workspace][probation] Docker-only (v5.0.0) SILENTLY BROKE the web folder picker: `listDirs` (api-data.ts) does `readdirSync` on the SERVER's fs, which inside the container is only the mounted paths (~/.huu, ~/Downloads, $PWD) — navigating "up" in the picker hits empty container mount points, not the host tree, so the user can't reach projects elsewhere. FIX = a WORKSPACE ROOT bind-mounted RW at the SAME absolute path: `docker-reexec.ts` `resolveWorkspaceRoot(hostHome, env)` = `HUU_WORKSPACE` (existence-checked, degrade to $HOME) → `process.env.HUU_WORKSPACE` set before reexec + added to the passthrough Set → mounted via `extraMounts`, deduped against nested state mounts with `isPathInside` (both new + exported for tests). Server: `workspaceRoot()` = `HUU_WORKSPACE || opts.cwd` (cwd fallback preserves pre-workspace behavior for dev/tests run WITHOUT the wrapper, i.e. HUU_IN_CONTAINER=1 direct-server); `/api/bootstrap` exposes `workspace`, a bare `/api/folders` (no ?path) opens there. Client: `S.boot.workspace` drives the picker's initial `loadFolder` + a ⌂ Home button. NON-OBVIOUS: (1) the mount is RW and decided at container START (single long-lived server → can't mount per-run), so the whole workspace is reachable by every run's agent shell — a DELIBERATE isolation relaxation, default $HOME, documented with a security note; keep it as small as projects allow (HUU_WORKSPACE=~/Projects). (2) verify the picker WITHOUT docker by running the server with HUU_IN_CONTAINER=1 + HUU_WORKSPACE=<real path> (bypasses the docker gate, exercises the exact server+client path); the real mount needs a full wrapper e2e (docker inspect Mounts shows `$HOME -> $HOME`, and the in-container /api/folders lists host home dirs). (3) default-branch discipline held: feature committed post-release on main + ff'd to development, NOT pushed (ships in the next version, sits in CHANGELOG [Unreleased]).
- [2026-07-03][source:user][task:web-options-key-and-terminal-logs][probation] SUPERSEDES the "browser-only, never disk" web key policy ([task:openrouter-key-401-shadow], user's own earlier call): after a stale saved key 401'd every run with NO way to fix it from the web (and no feedback anywhere), the user asked for a ⚙ Settings "OpenRouter API key" panel that VALIDATES then PERSISTS. Flow: `POST /api/keys/validate` → only if not `invalid` → `POST /api/keys` (disk via saveApiKey + `manager.setWebKey` live override) + `setSessionKey` for this tab; `invalid` is never saved; `unverifiable` (offline) saves with a warning (validation-never-blocks). Status via NEW `GET /api/keys/status?name=` (source `options|stored|secret-mount|env-file|env|none` + `maskKey`'d value — never raw, pinned by a server.test regression) and `DELETE /api/keys?name=` (clears store + override, reports the fallback tier + a restart note when a secret mount still holds the old value). The launch-form session-key rows are UNCHANGED and still win per run (`pickRunKey`: request > options > resolver). CSS gotcha: `.key-status` is flex-ROW — block `.key-hint` children inside it need `.settings-modal .key-status { flex-wrap: wrap }` + `.key-hint { flex-basis: 100% }` or they squash side-by-side.
- [2026-07-03][source:user][task:web-options-key-and-terminal-logs][probation] The serve TERMINAL now narrates (user: "qualquer problema ou acerto quero os logs no terminal do npm start"): new leaf `src/web/terminal-log.ts` — `termLog(level, scope, msg)` with a swappable writer whose DEFAULT is VITEST-silenced (suites construct real WebRunManagers; without the guard every test run interleaves run chatter) — plus `OrchestratorOptions.onLog` (a tee called after each activity-log append; debug filtered out, agent/merge/judge tagged, stage-prefixed) wired per run in `run-manager.start()`. Lifecycle logs: queued (with the masked key + source description + priority), started (queue wait), finished (duration + Σcost), FAILED (first line), abort, refused POSTs, key validate/save/clear, RAM-dial changes, request 500s. Raw firehose stays OFF by default — `HUU_WEB_LOG_STREAM=1` opts in (it's per-line agent prose and drowns the lifecycle log). Client half of the same complaint: `postRun`'s catch and `onRunFrame`'s `phase:'error'` transition now `toast(...)` (the `processed` Set already dedupes the frame path) — before, a refused/early-dying run only recolored a queue chip. E2E gotcha rediscovered: pipeline files must be named `<name>.pipeline.json` — the fixture's own filename `smoke-pipeline.json` does NOT resolve; copy it as `smoke.pipeline.json`.
- [2026-07-03][source:user][task:board-stuck-on-terminal-run][probation] URGENT user bug ("está funcionando mas a interface web não mostra nada", 9 Fleury projects narrating in the serve terminal, browser parked on the launch view): the active-run pointer (`S.activeRunId`) adopted the OLDEST tracked run and only re-adopted when the pointed run VANISHED (`if (!S.activeRunId || !S.runs.has(S.activeRunId))`), while the board auto-switch fires only when the POINTED run has `phase==='running'` — so ONE early terminal run (e.g. a 401-failed first attempt, or project 1 finishing while the user was elsewhere) permanently parked the UI on a dead run: `active=false` every frame, board never opens, no errors anywhere. FIX: pure `client/run-select.js pickActiveRun(currentId, pinnedId, runs)` (pin > running current > first running > queued current > first queued > current > most recent), folded in `ingestRun` on EVERY frame; the selector's `pickRun` sets `S.runPinnedId` (explicit choice wins until its run leaves tracking). Spec'd by `src/web/run-select.test.js` incl. a bootstrap-replay fold test. Lesson: any "adopt once, move only on disappearance" pointer over a Map that accumulates TERMINAL entries will eventually point at a corpse — make selection a pure function of (pointer, pin, live set) recomputed per frame.
- [2026-07-03][source:inference][task:board-stuck-on-terminal-run][probation] REAL-BROWSER verification recipe that found the bug in one shot (upgrades "verify with no browser" — sometimes you DO need one): scratch dir → `npm i playwright-core` + `npx playwright-core install chromium` (~170MB, no repo deps), then a diag script that POSTs stub runs, `page.goto`, collects `console.error` + `pageerror`, `page.evaluate` DOM probes (`viewRun.hidden`, `.card` count, status pill) and `page.screenshot` — the screenshot IS readable by the agent, so you can SEE the blank board / fixed kanban. Zero console errors + wrong view state = LOGIC bug, not a crash; a crash would show in `pageerror`. Also confirmed serveStatic already sends `Cache-Control: no-cache`, so stale HTML/JS pairs are NOT a live failure mode — but the new ⚙-panel listeners were made null-safe anyway (a bare `$('missing').addEventListener` at module top level kills the ENTIRE app when markup and script versions ever diverge).
- [2026-07-03][source:inference][task:kanban-done-means-merged][probation] New pure client module src/web/client/card-state.js (agentCardState → {lane, cls, plabel}) mirrors src/lib/card-state.ts — twin table tests (src/web/card-state.test.js) keep the mirrors in lockstep; app.js agentCard() delegates. New .phase.ready (--teal) and .phase.unmerged (--yellow) tokens in styles.css; paused → lane 'todo' now. History: db.js buildHistoryCards carries merged/mergeFailed (undefined on legacy IndexedDB archives → unchanged rendering) and the table's phase cell appends "(unmerged)".
- [2026-07-03][source:inference][task:host-aware-ram][probation] budgetTelemetry() (and thus the 1Hz SSE budget frame + bootstrap) gained containerAware/hostTotalBytes/hostAvailableBytes/hostClampActive/noKernelCeiling — the chip title explains container-vs-host scope when they differ >3%, renders a 'host-limited' lvl span while the clamp binds, and toasts ONCE on noKernelCeiling (client-side warned flag). Additive fields flow through run-manager's spread untouched.
- [2026-07-03][source:user][task:host-ram-headline][probation] Honesty must live in the HEADLINE, not the tooltip: the budget chip's visible used/total pair was container-scoped (huu-only) and the host truth was hover-title-only — the user kept reading the machine as emptier than it was ("sempre parecia que tava menos"). renderBudget now renders BOTH pairs as chip spans (huu X/Yg · host U/Tg, host = hostTotal − hostAvailable from budgetTelemetry). Rule of thumb: when a metric's SCOPE differs from what the user thinks they're reading, relabel it inline (huu/host prefixes) — a tooltip never corrects a headline.
- [2026-07-03][source:agent][task:sse-zombie-watchdog][probation] Native EventSource auto-reconnect fires ONLY on a transport ERROR — a half-open (zombie) connection keeps `readyState===OPEN` while delivering nothing, and with `es.onerror` a no-op the whole multi-run board froze until a manual refresh (the "Firefox não atualiza" incident). Three coupled facts make the fix shape non-obvious: (1) SSE COMMENTS (`: ping`) are invisible to the EventSource API, so the old 25s keep-alive could never feed a client watchdog — it had to become a REAL `event: ping` (writeSse already supports named events; old clients ignore them; `WebServerOptions.heartbeatMs` makes it testable); (2) the `{type:'budget'}` 1 Hz frame is NOT a usable liveness signal — `server.ts` skips it whenever `manager.budgetTelemetry()` is null (no scheduler yet, `/simulation`, post-abort); (3) a CLOSED EventSource (non-200/token change) is PERMANENT — the browser never retries, only a NEW EventSource object recovers. The watchdog lives in the pure DOM-free `client/sse-liveness.js` (staleMs 60s = 2×ping+margin; backoff 1s→30s reset ONLY by a received frame, so `connected()` on a dead server keeps escalating) + a single module-level 5s interval in app.js; forced reconnects re-fetch `/api/bootstrap` (the connect replay already re-syncs — bootstrap is the belt) and `visibilitychange`/`online` trigger an immediate check (suspend-wake is exactly when zombies surface). Server hardening: `server.requestTimeout=0`, `headersTimeout=60_000`. Verified END-TO-END in headless Chromium against a real child-process server: `kill -STOP` (socket open, zero frames) → watchdog warned and reconnected; `kill -CONT` → a NEW stub run's firehose reached the same page without any refresh.
- [2026-07-03][source:agent][task:queue-v2-relink][probation] The queue runner's tracking (`q.running`/`q.live`/`q.processed`/`q.settled`) was memory-only AND `persistQueue` stripped `status` while never running after `postRun` assigned `item.runId` (startQueue nulls runIds then persists → the stored runId was ALWAYS null). A mid-queue F5 therefore reset every chip to 'pending', stopped IndexedDB archiving/queue completion, and re-armed "Run queue" against projects still running server-side (duplicate-dispatch hazard) — the "só vejo a história daquele item" half of the incident. Fix: versioned `{schema:'huu-queue-v2', queueId, items}` envelope in localStorage KEEPING status+runId (still credential-free — keys resolve from sessionStorage at dispatch), `persistQueue()` at every item transition, and a pure `relinkQueue(items, runs)` in client/queue-util.js run at boot (S.runs is guaranteed populated first — boot order ingest→connectSse→restoreQueue): exact runId match adopts the server phase (terminal → re-archive; History put is idempotent because `buildRunRecord` keys `id: run.runId`); a runId the server lost ⇒ huu restarted (non-terminal runs are never pruned) → synthetic error archive; status queued/running WITHOUT a runId (refresh ate the POST response — the server registers the queued seed BEFORE responding) → heuristic claim of an UNCLAIMED NON-TERMINAL run by (runDirectory,pipelineName), each run claimable once, terminals deliberately never claimed (stale done-runs for the same project are common across queue executions) → else re-dispatch. Tracking Sets rebuild SYNCHRONOUSLY before the async archive/dispatch tail so replayed terminal SSE frames can't double-archive. Legacy bare-array payloads keep the old reset-to-pending. Verified e2e: seeded v2 envelope + reload mid-run → chips restored running, Stop-queue visible, run archived to IndexedDB by the relinked queue.
- [2026-07-28][source:inference][task:dev-mode-v2][probation] Five key-POOL endpoints now sit beside the single-key block in `server.ts`: `GET /api/keys/pool` (state), `POST /api/keys/pool` (add), `DELETE /api/keys/pool?name=&index=` (remove), `POST /api/keys/pool/reset` (clear burns/cooldowns, whole pool or one index), `POST /api/keys/pool/validate` (re-probe a STORED key and RECORD the answer). Two invariants to preserve when touching them: they are INDEX-addressed and MASK-returned — the browser never receives a key value, only `maskKey(value)` plus rotation state (`active` / cooling-until / burned) — and writes are validate-then-persist, exactly like the single-key panel, because a key the provider rejects would otherwise sit in the pool costing a wasted rotation on every later failure. One real trap already fixed and worth not reintroducing: `url.searchParams.get('index')` yields `null` when absent and `Number(null)` is `0`, so an index-less DELETE silently dropped key #0 — the param has to be checked for PRESENCE before coercion. `validate` deliberately records rather than merely reports, since probing without recording leaves the badge stale, which is the thing it exists to fix.
- [2026-07-28][source:inference][task:dev-mode-v2][probation] The dev session's two new interactive gates (resume an earlier session, land orphan integration branches) reuse the approval gate's PARKED-PROMISE pattern rather than inventing anything: the driver callback returns a promise, `DevManager` stores its resolver (`resumeResolve` / `orphanResolve`) and flips a boolean on the session frame (`awaitingResume` / `awaitingOrphans`) so the SSE snapshot alone tells the browser what to render; `POST /api/dev/resume` and `POST /api/dev/orphans` call the resolver and return false when nothing was waiting. The rule that makes it safe is that both must FAIL CLOSED on abort — resume answers `false` (a fresh session) and orphans answer `'ignore'` (nothing is merged behind the user's back) — and `abort()` has to release EVERY parked gate, not just the approval one, or an aborted session hangs forever on a promise nobody will resolve. Same shape for any future gate: state on the frame, resolver on the manager, closed answer on abort.
- [2026-07-30][source:inference][task:playbook-ondas][probation] Splitting a 3723-line single-file JS client into ES modules (M7-01: app.js → 7 modules) exposed that circular dependencies (launch ↔ board, launch ↔ dev, launch ↔ queue) work via ES module live bindings ONLY when imports are used inside function bodies, never at module evaluation time. The typecheck caught 5 missing cross-module imports (`fanOutBatch` from wrong module, `wireModeSwitch`, `boot`, `renderDevSession`, `renderQueue`+`keyStoreName`) that would have been runtime errors. A `node --check` pass on each module is the minimum gate after any JS split.
- [2026-07-31][source:inference][task:dev-mode-metodologias][probation] The methodology checkboxes follow the data-driven catalog pattern end-to-end: `DEV_METHODOLOGY_OPTIONS` lives in server.ts typed `keyof DevMethodology` (the catalog breaks at compile time if the type changes) and rides `/api/bootstrap` like devModelRoles; the client renders toggles from the catalog (panel hides when the catalog is absent — same stale-build degradation as the role panel), keeps state in a Set, and persists by mirroring into `S.settings.devMethodology` + `saveSettings()` so a save from the ⚙ modal can't wipe the selection. Pure logic went into `client/dev-methodology.js` (payload + storage parsing, filtering stored keys against the live catalog) with a colocated `.test.js` run by vitest's node environment — the convention for client logic. Two contracts that matter: the POST body OMITS the `methodology` key entirely when nothing is on (byte-identical pipelines), and server coercion keeps only values literally `=== true` under known keys. In the client typecheck (243 pre-existing errors; the working rule is never add), `ev.target instanceof Element` + `getAttribute` avoids the TS2339 that the `ev.target.closest(...)` house pattern produces — check the error count before/after any client edit. UI language note: the dev form is English, so new panels are English too ("Methodology"), even when the feature's docs are pt-BR-first.

- [2026-08-02][source:inference][task:web-challenge-selector][promoted] CORRECTION to this skill's body: `tsconfig.client.json` has `allowJs+checkJs` and `npm run typecheck` runs `tsc -p tsconfig.client.json --noEmit`, so client `.js` modules ARE typechecked (the "no allowJs, client .js is NOT type-checked" line is stale). Consequence: `el.querySelector(...)` returns `Element`, so any `.value`/`.hidden`/`.disabled` access needs the house JSDoc cast — `/** @type {HTMLInputElement | null} */ (el.querySelector('.x'))` — and `ev.target` needs `ev.target instanceof Node` before `el.contains(ev.target)` (TS2345). A `node --check` + `npx tsc -p tsconfig.client.json --noEmit` is the per-module gate.
- [2026-08-02][source:inference][task:web-challenge-selector][probation] The bundled pentest `web-search.sh` is NOT executable in the src tree (chmod only happens when skills materialize into `.huu/pentest/skills/`), so server-side calls must be `execFile('/bin/bash', [script, query, count], {timeout})` — never execFile(script) directly (EACCES). The script's exit contract: 3 = no BRAVE_API_KEY/secret → map to 503 `{error:'BRAVE_API_KEY not configured'}`; 127 = curl/jq missing; ETIMEDOUT/killed = timeout (the script's own curl cap is 20s, bind the call tighter). The script path resolves dev/prod via `new URL('../lib/pentest-skills/…', import.meta.url)` — the build copies the whole tree into dist/lib/, same trick as clientDir(). Parsing: output is `• title / <2-space url> / description` blocks; descriptions can span lines.
- [2026-08-02][source:inference][task:web-challenge-selector][probation] `check-metodo` in the gate FAILS (not warns) when a §3 singleton file grows >10% from its METODO.md number — server.ts at +13% tripped it. Fix: update the METODO.md §1/§3 derived numbers AND the header (`Data de escrita` + commit) to the printed real values; the header mismatch itself is only a warning. Also `smoke-defaults` exits 2 when `dist/` is missing — run `npm run build` before `bash scripts/gate.sh` in a fresh worktree.
- [2026-08-04][source:inference][task:devgraph-web-surface][probation] `readJsonBody` (server.ts) THROWS on a malformed or oversized body, and `createServer`'s catch-all turns that throw into a 500 — i.e. the CALLER's bad JSON is reported as huu's fault. `readJsonBodyOr400(req, res)` is the wrapper that answers 400 and returns `undefined`; the call site does `const body = await readJsonBodyOr400(req, res); if (!body) return;`. ~12 legacy routes still call the bare `readJsonBody`, so the pattern is NOT enforced by the type system — every NEW route that reads a body must reach for the `Or400` variant deliberately. Verified: `/api/dev/start`, `/api/dev/approve`, the four `/api/keys/pool` routes and the whole `/api/graphs` branch use it; `/api/run`, `/api/keys`, `/api/run/abort` still do not.
- [2026-08-04][source:inference][task:devgraph-web-surface][probation] A route SEGMENT that is also a legal resource id (`/api/graphs/catalog`, `compile`, `validate`, `from-sample` — all valid slugs) makes a resource that can be SAVED and then never READ, because the route match wins over the `:id` match. The fix that closes it is to refuse the id on the WRITE path, not to drop the route: `GRAPH_RESERVED_SEGMENTS` (`src/web/graph-api.ts`) is checked inside `writeGraphResult` and answers `invalid-id: … a graph with this id could be saved but never read back` (400), so the ambiguity is reported when it is CREATED instead of discovered on the next click. Renaming the read route instead would only move the collision.
- [2026-08-04][source:inference][task:devgraph-web-surface][probation] `vitest.config.ts` declares NO `environment`, so every suite — including client `*.test.js` — runs under the default `node` pool with no `document`/`window`. A client test that must touch the DOM opts in PER FILE with a first-line `// @vitest-environment jsdom` (jsdom is a devDependency, added for this; the only two users today are `client/modules/graph/canvas.test.js` and `inspector.test.js`). Do NOT set a global `environment: 'jsdom'` — the rest of the suite is server code that boots real HTTP servers and real git repos, and would pay jsdom's cost per file.
- [2026-08-04][source:inference][task:devgraph-web-surface][probation] The no-CDN vendor rule has TWO shapes, and the criterion is whether the UPSTREAM dist is self-contained. `motion.js` is the upstream UMD build copied RAW (`cp node_modules/motion/dist/motion.js`) because its ESM entry re-exports from `framer-motion/dom` and is unusable without a bundler. `reactflow.js` is PRE-BUNDLED BY US (`scripts/vendor-reactflow.sh`, esbuild, `@xyflow/react` + one React runtime + ~N npm packages inlined, licenses collected into `vendor/LICENSE.md`, banner cross-checked by `vendor/reactflow.test.js` against `vendor/README.md`) because no upstream artifact is a single self-contained ESM file. Ask that question first: a raw copy for a self-contained dist, a generator script committed alongside the output for everything else.
- [2026-08-04][source:inference][task:devgraph-web-surface][probation] Coercion discipline on a request body is NOT uniform — it depends on what the FALLBACK is. `/api/dev/start` DROPS a malformed `models`/`modelsPreset`/`methodology` (the fallback is the default the caller would have got anyway) but 400s on a `graph`/`graphId` that is present and unusable, because there the fallback is the LLM PLANNER — silently swapping the human's drawn topology for a model's, the one thing every other layer refuses to do. `null` and a whitespace-only `graphId` are the deliberate exception (a client clearing its picker). Rule: drop what degrades to the same default, refuse what degrades to a DIFFERENT actor.
- [2026-08-04][source:inference][task:onda7-launch-from-web][probation] THE WEB UI WAS DEAD ON LOAD, on every route, and no test could see it. `modules/launch.js` built `resolverCombo` at MODULE level with `placeholder: t('web.config.resolver_placeholder_value')`; the browser `t()` THROWS on a key it cannot find, and at module-evaluation time the catalog is still `{}` because `boot()` only awaits `initI18n()` after the whole graph is evaluated. The throw killed the `<script type="module">` before anything rendered. `translating-the-ui` already documents "module-level `t()` translates at IMPORT time" — the gap was DETECTION: not one test imported `app.js` or `launch.js`, so the gate was green over a dead page. Fix: `placeholder`/`emptyHint` are now FUNCTIONS called from `syncInput`/`updateHint`, and `modules/dev.test.js` imports the real entry chain so a relapse fails the suite. Rule: at least one test must import the browser ENTRY with an empty catalog.
- [2026-08-04][source:inference][task:onda7-launch-from-web][probation] A jsdom suite can drive the WHOLE client, not just a pure module: read `client/index.html`, slice its `<body>`, assign it to `document.body.innerHTML` (scripts inserted that way never execute, so `app.js` does not boot twice), stub `globalThis.EventSource` + `ResizeObserver`, replace `globalThis.fetch` with a `path → handler` router that RECORDS every request, and only THEN `await import('./dev.js')`. That makes "the POST carried `graphId` and never `maxEpochs`" an assertion about the wire instead of about a helper, and it fails when an id is renamed in the markup. Two gotchas: jsdom replaces the global `URL`, so `fileURLToPath` must be given `import.meta.url` as a STRING (`dirname(fileURLToPath(import.meta.url))`, never `new URL('../x', import.meta.url)`); and `dev.js` runs `wireDev()` + `boot()` at import time, so the module is imported ONCE per file and tests reset shared module state themselves.
- [2026-08-04][source:inference][task:onda7-launch-from-web][probation] `backendSpecName(id, boot)` needs BOTH arguments — it reads `boot.backends`. `modules/dev.js` and `launch.js:143` call it with one, so `sessionKey(undefined)` returns `''` and the /dev POST has ALWAYS sent `apiKey: undefined`; only `queue.js:241` passes `S.boot`. Left alone deliberately while proving "a session with no drawing posts the same bytes", but it is a real bug: a browser-session key never reaches a dev run, which only works because the server falls back to the config-store key.
- [2026-09-05][source:inference][task:onda2-web-dev-surface][probation] The `agent-stream` firehose is the ONLY live source for content agents write to a file: the file itself only reaches its canonical path after the wave's merge, so watching it shows the finished result and never a live fill.
- [2026-09-05][source:inference][task:onda2-web-dev-surface][probation] `updateQueueChrome()` runs at the END of `renderActiveRun()` and overwrites `backToLaunch.textContent`; any custom label on that element must be applied AFTER it, not before.
- [2026-09-05][source:inference][task:onda2-web-dev-surface][probation] `src/web/client/modules/state.js` reads `location.search` at module body scope, so it cannot be imported under Node — a client module that needs a Node test must not import it.
